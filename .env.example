# Optional: Hugging Face Hub token for higher rate limits and reliable downloads.
# Create a token at https://huggingface.co/settings/tokens
# Copy this file to `.env` and set your value (`.env` is gitignored).
HF_TOKEN=
